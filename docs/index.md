# 组件文档

欢迎使用我们的 Vue3 组件库文档！

## 快速开始

这个项目包含以下特性：

- 📦 **组件源码**: `src/` 目录存放组件源码
- 📚 **文档**: `docs/` 目录存放组件文档
- 🎯 **示例**: `demo/` 目录存放组件使用示例
- 🔄 **自动生成**: API 文档和 Changelog 自动生成

## 项目结构

```
component-docs/
├── src/                    # 组件源码
│   ├── components/         # 组件文件
│   └── index.ts           # 入口文件
├── docs/                  # 文档
│   ├── .vitepress/        # VitePress 配置
│   ├── components/        # 组件文档
│   └── index.md           # 首页
├── demo/                  # 示例
│   └── examples/          # 组件示例
└── scripts/               # 构建脚本
```

## 开发指南

1. 在 `src/components/` 中开发组件
2. 在 `demo/examples/` 中创建使用示例
3. 在 `docs/components/` 中编写组件文档
4. 运行 `npm run api:generate` 自动生成 API 文档
5. 运行 `npm run changelog:generate` 自动生成更新日志

## 开始使用

```bash
# 安装依赖
npm install

# 启动开发服务器
npm run dev

# 构建文档
npm run build
```
